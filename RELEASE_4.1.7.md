# Arctic Foxes 4.1.7

Following a player now shows a compact name-and-number flag near the helmet, with a short pointer, instead of a rectangle around the body. Flags stay inside the frame near its edges.

Player setup, tracking safeguards, bench crossings, and whistle-time controls continue to work as in 4.1.6. Following remains a preview and requires reconnection when identity is uncertain. Existing saved data and the stable storage location are preserved.

Validation: isolated Electron film preview checked the flag appearance and existing film controls. The automated regression suite runs before publication.
