const {app,BrowserWindow}=require('electron');const path=require('path');const fs=require('fs');
app.setPath('userData',path.join(__dirname,'../dist/visual-check/profile-'+Date.now()));
app.whenReady().then(async()=>{const w=new BrowserWindow({width:1440,height:1000,show:true,title:"Arctic Foxes — Design Preview (sample data)",webPreferences:{contextIsolation:true,backgroundThrottling:false}});w.webContents.on('console-message',(_e,_l,m)=>console.log(m));await w.loadFile(path.join(__dirname,'../index.html'));await new Promise(r=>setTimeout(r,6500));fs.mkdirSync(path.join(__dirname,'../dist/visual-check'),{recursive:true});
await w.webContents.executeJavaScript("document.querySelector('#createGameDate').value='2026-08-16';document.querySelector('#createOpponent').value='Preview game';createSimpleGame();state.officialStats=emptyOfficialStats();state.players.filter(p=>p.pos!=='G').forEach((p,i)=>{state.officialStats.skaters[p.number]={number:p.number,name:p.name,gp:1,g:i%3===0?1:0,a:i%2,shots:3,plusMinus:1,pim:0,blocks:1,fo:4,fow:2};});state.officialStats.imported=true;state.officialStats.team={goalsFor:{total:3},goalsAgainst:{total:1},shotsFor:{total:26},shotsAgainst:{total:18},ppChances:4,ppSuccess:1,pkChances:3,pkSuccess:3,fo:40,fow:23,fol:17};state.savedGames.find(g=>g.id===state.currentGameId).officialStats=JSON.parse(JSON.stringify(state.officialStats));render();");
for(const [name,code] of [['home',"openWorkspace('home')"],['jerseys',"dashboardView318='jerseys';openWorkspace('home')"],['film',"openWorkspace('film')"],['develop',"document.querySelector('#workspacePlayerValue').click()"],['stats',"document.querySelector('#closePlayerValue').click(); document.querySelector('#createGameDate').value='2026-08-16';document.querySelector('#createOpponent').value='Preview game';createSimpleGame();openWorkspace('quickstats')"]]){await w.webContents.executeJavaScript(code);console.log(name,await w.webContents.executeJavaScript("JSON.stringify({game:state.currentGameId,count:state.savedGames.length,page:activeWorkspace,head:document.querySelector('#commandCenter313').innerText.slice(0,160),nav:[...document.querySelectorAll('#workspaceNav button.active')].map(b=>b.textContent)})"));w.webContents.invalidate();await new Promise(r=>setTimeout(r,1200));fs.writeFileSync(path.join(__dirname,'../dist/visual-check/'+name+'.png'),(await w.webContents.capturePage()).toPNG());}
await w.webContents.executeJavaScript("window.scrollTo(0,document.body.scrollHeight)");await new Promise(r=>setTimeout(r,300));fs.writeFileSync(path.join(__dirname,'../dist/visual-check/team.png'),(await w.webContents.capturePage()).toPNG());app.quit();}).catch(e=>{console.error(e);app.exit(1)});
